Installation instructions
==========================
-To begin operation of the chess game Your move. Make sure you're in the Chess_V1.0_src directory and then
enter the binary directory via the linux terminal command:
cd bin

To compile the game Your move. Enter the following command into your linux terminal:
make

To run the game enter the following command:
./chess